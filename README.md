# Orderbook

This is an orderbook implementation in C++. To run this project, clone this repository and add the two [catch2 amalgamated files](https://github.com/catchorg/Catch2/tree/devel/extras) into a directory called `catch2`. Then, simply run `make exec && ./bin/exec` or `make tests && ./bin/tests`.